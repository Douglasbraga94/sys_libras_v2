
exports.up = function(knex, Promise) {
  return knex.schema
    .createTable('papel', table => {
        table.integer('codigo').primary();
        table.string('nome', 50).notNullable();
        table.integer('prioridade').notNullable();
        table.boolean('curso', false).notNullable();
        table.boolean('ativo', true).notNullable();
    })
    .raw('ALTER TABLE papel ALTER COLUMN codigo ADD GENERATED BY DEFAULT AS IDENTITY');
};

exports.down = function(knex, Promise) {
  return knex.schema
    .dropTable('papel');
};
