/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  return knex.schema
    .createTable('colaborador_competencia', table => {
        table.integer('codigo').primary();
        table.string('nome', 255).notNullable();
        table.boolean('ativo', true).notNullable();
    })
    .raw('ALTER TABLE colaborador_competencia ALTER COLUMN codigo ADD GENERATED BY DEFAULT AS IDENTITY');
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
    return knex.schema
        .dropTable('colaborador_competencia');
};
