/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
    return knex.schema
        .createTable('evento', table => {
            table.integer("codigo").primary();
            table.integer('codigoLocalidade').references('Codigo').inTable('relatorio.Localidade');
            table.integer('codigoTurma').references('codigo').inTable('turma');
            table.date('dataEvento').defaultTo(knex.fn.now()).notNullable();
            table.time('horaInicio').notNullable();
            table.time('horaTermino').notNullable();
            table.boolean('ativo', true).notNullable();
        })
        .raw('ALTER TABLE evento ALTER COLUMN codigo ADD GENERATED BY DEFAULT AS IDENTITY');
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
    return knex.schema
        .dropTable('evento');
};
